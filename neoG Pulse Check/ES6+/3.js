const findFirstAndSecond=(arr)=>{
    let first=arr[0];
    let second=arr[1];
    const requiredObject={first,second};
    return requiredObject;
  }
  
  const arr=[1,2,3,4,5]
  console.log(findFirstAndSecond(arr));